name: Testando o Trigger do tipo delete

on:
  delete

jobs:
  print-branch-or-tag-delete:
    runs-on: ubuntu-latest
    steps:
      - name: Avisa sobre remoção da branch ou tag
        run: echo "Uma branch ou tag foi removida no repositório"
