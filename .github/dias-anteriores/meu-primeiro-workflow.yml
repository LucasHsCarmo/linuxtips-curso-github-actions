name: Primeiro Workflow LinuxTips

on:
  push:
    branches:
      - main

jobs:
  giropops:
    runs-on: ubuntu-latest
    steps:
      - name: Confere versao do Ubuntu
        run: cat /etc/os-release
      
      - name: Exibir mensagem
        run: echo "Hello World - GH"